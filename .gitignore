# Rust
/target
**/*.rs.bk

# Built frontend — regenerated by `make ui`, embedded at compile time by rust-embed.
# build.rs writes a placeholder index.html here so a clean clone still compiles.
/web/dist
/web/node_modules
/web/coverage
/web/*.tsbuildinfo

# Env / editor
.env
.env.*
!.env.example
.idea
.vscode
*.sw?
.DS_Store
